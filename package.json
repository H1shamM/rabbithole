{
  "name": "stumble-clone",
  "private": true,
  "scripts": {
    "test:e2e": "npx playwright test",
    "prepare": "husky",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "eslint": "^10.4.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.8.3"
  },
  "lint-staged": {
    "app/**/*.{js,ts}": [
      "eslint --fix",
      "bash -c \"cd app && if npm run typecheck --if-present; then echo ''; else echo 'No typecheck script'; fi\""
    ],
    "ui/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "bash -c \"cd ui && if npm run typecheck --if-present; then echo ''; else echo 'No typecheck script'; fi\""
    ]
  }
}
